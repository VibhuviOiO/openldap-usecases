version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    # Bundle every action bump into a single PR instead of one per action.
    groups:
      github-actions:
        patterns: ["*"]
